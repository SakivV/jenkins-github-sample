pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'This is build phas-3'
            }
        }
        stage('Test') {
            steps {
                echo 'Test phase-3'
                // Example: Run tests
                // sh './run-tests.sh'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy Phase-3'
                // Example: Deploy to production
                // sh './deploy.sh'
            }
        }
    }
}
